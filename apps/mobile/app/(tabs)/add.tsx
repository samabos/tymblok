import { View } from 'react-native';

// This is a placeholder screen for the Add tab button
// The actual add functionality is handled by a modal triggered from the tab bar
export default function AddScreen() {
  return <View />;
}
